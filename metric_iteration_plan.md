# 中文诗歌「诗歌性」自动评测指标 —— 组合与迭代方案

> 版本：v1.0（基于 Stage 1 三轮 + Stage 2 十一轮实验的完整迭代方案）
> 日期：2026-08-14
> 关联日志：`04_memory/experiment_logs/round_001~003.json`, `stage2_round_001~011.json`

---

## 一、方案定位

**目标**：构建一个「诗歌 / 非诗歌」二分类的自动指标，使其与人类判断（clean human labels）高度一致，并明确其在 AI 仿诗上的失效边界。

**核心思想（来自 13 轮实验的教训）**：

1. **指标 ≠ AI 检测器**：指标回答「这是不是诗」，不回答「这是不是 AI 写的」。
2. **指标必须按文本判断，不按元数据**：生产 DB 的 title/author 严重错配，标注者按元数据投票产生 ~30% 噪声。
3. **指标迭代 = 发现失败模式 → 收集反例 → 重训**：真正的闭环。

---

## 二、当前指标体系（v2 冻结 + v4 迭代候选）

### 2.1 特征库（21 个自有特征 + 5 个 baseline = 26 维）

| 族 | 特征 | 说明 |
|---|---|---|
| **form** (4) | line_count, line_char_var, classical_match, n_lines_score | 格律 / 行数 |
| **struct** (3) | n_lines, line_ending_punct, short_line_ratio | 分行 / 标点 |
| **jump** (3) | connector_density, char_per_line, line_density_var | 逻辑跳跃（粗代理）|
| **lang** (4) | imagery_density, classical_marker_density, prose_particle_density, line_break_existence | 意象 / 文言 |
| **purity** (4) | han_ratio, no_english, no_digit, line_cleanliness | **v3 新增**：文本纯净度 |
| **music** (3) | pattern_regularity, ping_ze_balance, final_char_ping_ratio | 平仄（普通话简化）|
| **baseline** (5) | bleu_to_poem/nonpoem, bigram_jacc_to_poem/nonpoem, tfidf_cos_to_poem | 平凡解参照 |

### 2.2 分类器

LogisticRegression（C=1.0, class_weight=balanced, max_iter=3000, seed=42）+ StandardScaler。

### 2.3 版本状态

| 版本 | 训练数据 | 状态 | val kappa | AI 诗 kappa |
|---|---|---|---|---|
| **v2 (frozen)** | 1500 诗 + 355 非诗 | ✅ 冻结（Stage 2 用）| 0.940 | -0.012 |
| v3 | + purity 特征 | ⚠️ 无效（无 AI 垃圾训练样本）| 0.940 | -0.012 |
| **v4b** | + 32 条 AI 垃圾负样本 | ✅ 迭代候选 | **0.974** | **0.431** |

**v4b 结论**：把「人类判定为非诗的 AI 仿诗」加入训练负样本后：
- AI 诗上的假阳性 31 → **0**
- 原 val kappa 0.940 → **0.974**
- 代价：AI 诗上假阴性 7 → 51（变保守）

---

## 三、指标组合方式（组合策略）

### 3.1 单指标组合（当前）

$$
\hat{y} = \sigma(w^\top z),\quad z = \text{StandardScaler}([f_1, ..., f_{26}])
$$

- 每个特征独立计算 → 标准化 → LR 加权求和 → sigmoid → 概率。
- **可解释性**：LR 系数 = 特征重要性（已输出到 `feature_coef.json`）。

### 3.2 组合的三个层次（提案）

| 层次 | 方法 | 用途 |
|---|---|---|
| **L1 单指标** | 26 维特征 + LR | 当前方案，可解释 |
| **L2 子指标** | 每个族先独立 LR 得子分数，再二次 LR 组合 | 可分离「格律贡献 / 意象贡献」，便于诊断 |
| **L3 混合** | L2 子分数 + 外部分数（LLM-as-judge / 困惑度）| 最终部署形态 |

**建议**：先做 L2，把 6 个族各自训成子指标，再组合。这能回答「人类判断诗歌时最依赖哪些特征」——正是方案 §1.3 的科学问题。

---

## 四、指标迭代闭环（核心机制）

### 4.1 闭环流程

```
┌─────────────────────────────────────────────────────┐
│  1. 部署当前指标 (vN)                                 │
│  2. 在新数据上运行 → 找「高置信错误」                  │
│     (指标极确定但人类判断相反)                         │
│  3. 人工复核错误样本 → 判定「指标错 or 人类错」         │
│  4a. 指标错 → 提取失败模式 → 加特征 或 加反例训练样本   │
│  4b. 人类错 → 修正标注 → 重新评估                      │
│  5. 重训 (vN+1) → 在 OLD 数据 + NEW 数据上评估         │
│  6. 对比 vN vs vN+1 → 通过则发布 vN+1                  │
└─────────────────────────────────────────────────────┘
```

### 4.2 触发条件（方案 §3.1「发现信号」映射）

| 发现信号 | 指标侧表现 | 动作 |
|---|---|---|
| 反例 | 指标高置信但人类反对 | 加入失败样本库 → 提取特征 |
| 异常 | 指标在某个 strat 上系统性失效 | 单独分析该 strat → 针对性加特征 |
| 稳定负结果 | 加了特征但 kappa 不变 | 放弃该方向，记录为负结果 |
| 失败模式 | 连续 3 轮无改善 | 回溯问题定义 / 特征空间 |

### 4.3 评估协议（每次迭代固定）

1. **固定 val split**：`train_val_split(seed=42, val_ratio=0.2)` —— 保证可比
2. **指标**：Accuracy / Quadratic Kappa / F1 / Brier / ECE
3. **参照系**：
   - Random baseline（0.028 kappa）
   - Human-IAA（clean：0.82；含噪声：0.39）
   - LLM-as-judge（stub：0.000；真 API 待接入）
4. **跨数据集**：原 val + 专家集 (samples.js 100) + AI 诗集 (209 human-labeled)

---

## 五、特征空间演进计划

### 5.1 已确认有效 / 无效

| 特征方向 | 实验 | 结论 |
|---|---|---|
| 结构类（form/struct） | R1-R2 | ✅ 有效但**偏向古典诗** |
| 语言学（lang） | R2 | ✅ `imagery_density` 进 Top-5 |
| 纯净度（purity） | R3 | ⚠️ 单独加特征无效（需训练样本配合）|
| 平仄（music 简化版）| R1-R3 | 中性（权重重度低）|
| baseline（bleu/rouge/tfidf）| R1-R3 | ✅ 字符重叠是主力（前 4 权重）|

### 5.2 下一轮特征候选（v5）

| # | 特征 | 目标失败模式 | 实现成本 |
|---|---|---|---|
| 1 | **新闻关键词密度** | 段落假阳性（nonpoem+social 17/17 人类对）| 低（词表）|
| 2 | **标点密度** | 古典诗 vs 现代散文 | 低 |
| 3 | **韵脚密度** | 古典格律诗 | 中（需韵表）|
| 4 | **困惑度 (perplexity)** | AI 检测（虽然非目标，但可作弱信号）| 高（需 LM）|
| 5 | **语义跳跃度（断裂-引力分布）** | 旧方案 §2 的核心原创想法 | 中（需 embedding）|
| 6 | **L2 子指标组合** | 可解释性 / 诊断 | 中 |

### 5.3 优先级建议

**P0（立即）**：新闻关键词密度 → 修 nonpoem+social 假阳性
**P1（下轮）**：标点密度 + 韵脚密度 → 强化古典诗识别
**P2（远期）**：断裂-引力分布 + perplexity → 深度语义 / AI 弱信号
**P3（研究）**：L2 子指标 + 可解释性分析

---

## 六、数据资源与标注质量控制（v5+ 前提）

### 6.1 可用数据清单

| 数据集 | 数量 | 标签 | 用途 |
|---|---|---|---|
| poetry-judge-train/neutral | 3000 | ✅ 二分类 | 训练 |
| samples.js 专家集 | 100 | ✅ 专家策展 | 独立评测 |
| eval-annotation 4 CSV | 1613 unique | ✅ 人类 | IAA / 噪声分析 |
| annotator_06 AI 诗 | 209 matched | ✅ 人类 | **AI 诗评测** |
| hard_dataset_5000 | 5000 | ❌ 无 | 补充 AI 诗池 |
| Neon DB | 109k | ❌ 未标注 | 未来标注池 |

### 6.2 标注质量控制（关键教训）

1. **隐藏元数据**：标注界面只显示 text（用户确认已经如此 ✅）
2. **一致性检查**：每个样本 ≥ 2 标注者，冲突自动标记
3. **噪声剔除**：指标置信度 ≥ 0.85 且与人类相反 → 疑似噪声 → 人工复核
4. **标注者培训**：明确「古典诗也是诗」+「不要因关键词投票」

### 6.3 已验证的噪声事实

- 所有 4 个标注者都有 ~30% 高置信噪声
- annotator_01（主力 96%）的关键词匹配偏差：看到「诗」字就投「是诗」
- 剔除噪声后：指标 vs 人类 kappa 0.39 → 0.92，IAA 0.39 → 0.82

---

## 七、方案 §3.1「发现信号」的实测响应

| 方案 §3.1 假设 | 实测 | 修订 |
|---|---|---|
| 「指标 vs 人类一致性随 AI 相似度↑ 下降」 | **指标从不区分来源**——接受率 97-99% 与相似度无关 | 假设不成立，改为「指标在 AI 垃圾上失效（v4 修复）」|
| 「人类 IAA 作为指标上界」| 含噪声 0.39 / 剔除后 0.82 | **IAA 必须先控噪声**再作为上界 |
| 「失败模式 → 回溯问题定义」| AI 垃圾（混英文）失败 → 加负样本 → v4 修复 | **特征空间不足 + 训练分布不足**，不需改问题定义 |

---

## 八、路线图

| 阶段 | 内容 | 交付 |
|---|---|---|
| **M1** (已完成) | v2 冻结 + 13 轮诊断 | round_001~011 + 6 份报告 |
| **M2** (本轮) | v4b 迭代：AI 垃圾负样本 | kappa 0.94→0.97 (val), 0→0.43 (AI 诗) |
| **M3** (下一轮) | 修 nonpoem+social 假阳性（新闻词密度）| v5 |
| **M4** | 标点/韵脚 + L2 子指标 | v6 + 可解释性报告 |
| **M5** | LLM-as-judge 接入 + 困惑度 | v7 + 完整对比 |
| **M6** | 论文写作 + 开源（GitHub/HF）| 终稿 |

---

## 九、一句话总结

> **指标迭代 = 「部署 → 找失败 → 判归属（指标/人类）→ 加特征或加反例 → 重训 → 验证」的闭环。**
>
> 本轮验证了闭环：发现 AI 垃圾诗失败模式 → 把 32 条人类标「非诗」的 AI 诗加入训练 → **v4b 假阳性归零、val kappa 升到 0.974**。
>
> 下一步（v5）：修 nonpoem+social 假阳性 → 加新闻关键词密度特征 → 再跑一轮闭环。