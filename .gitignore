# --- Python ---
__pycache__/
*.pyc
*.pyo
*.pyd
.Python
*.egg-info/
.eggs/
build/
dist/
*.so
.pytest_cache/
.mypy_cache/
.ruff_cache/

# --- Jupyter ---
.ipynb_checkpoints/
*.ipynb

# --- Editors / OS ---
.DS_Store
Thumbs.db
.vscode/
.idea/
*.swp
*.swo
*~
~$*.docx

# --- Secrets / credentials ---
.env
*.pem
*.key
secrets/
*.crt

# --- Heavy runtime artifacts (excluded from VCS) ---
.hf_cache/
*.safetensors
*.bin
*.pt
*.gguf
*.pkl
wandb/
runs/
checkpoints/

# --- Optional experiment intermediates (can be re-derived) ---
*.jsonl.gz
*.parquet
*.arrow

# --- Local-only references (project mentions but does not own) ---
# E:\生成诗歌\  is the source dataset; not part of this repo.
# (do NOT list as path; just a comment)

# --- Frozen metric snapshots (large; regenerated on demand) ---
# /05_experiments/stage2_hard_samples/round_*/frozen_metric.pkl

# --- Semantic embedding cache (large; regenerated by semantic_cache.py) ---
/07_reproducibility/semantic_cache.npz

# --- Entity embedding cache (large; regenerated by entity_cache.py) ---
/07_reproducibility/entity_vec_cache.npz

# --- DSH plugin install / web restart tooling (local, not repo content) ---
/00_docs/dump_config.log
/00_docs/plugin_install*.log
/00_docs/profile_backup_web/
/00_docs/restart_web.*
/00_docs/web_restart_script.log
/00_docs/web_start_*.log
/00_docs/wmi_run.txt
